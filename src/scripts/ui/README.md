The central design philosophy for UI is to decouple UI from logic by creating wrappers that extend the data/logic with draw functions. This will allow us to easily compose UI without fear of affecting underlying logic

